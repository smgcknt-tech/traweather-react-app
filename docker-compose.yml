version: "3"
services:
  nginx:
    depends_on:
      - frontend
      - backend
    restart: always
    build:
      dockerfile: Dockerfile.stage
      context: ./nginx
    ports:
      - "3000:80"
  frontend:
    environment:
      - CHOKIDAR_USEPOLLING=true
    build:
      dockerfile: Dockerfile.stage
      context: ./frontend
    volumes:
      - /app/node_modules
      - ./frontend:/app
    stdin_open: true
    tty: true
  backend:
    build:
      dockerfile: Dockerfile.stage
      context: "./backend"
    volumes:
      - /app/node_modules
      - ./backend:/app
    environment:
      - kabu_plus_user
      - kabu_plus_password
      - jwt_secret_key
      - AWSAccessKeyId
      - AWSSecretKey
      - API_PORT
      - PGUSER
      - PGDATABASE
      - PGHOST
      - PGPASSWORD
      - PGPORT
  postgres:
    image: "postgres:12.8"
    user: postgres
    environment:
      POSTGRES_PASSWORD: ${PGPASSWORD}
